// Previously present flat ESLint config disabled to avoid runtime parser errors.
// If you want to re-enable the flat config, restore the original file to eslint.config.mjs
// and ensure languageOptions.parser is an imported parser module (not a string), e.g.:
// import parser from '@typescript-eslint/parser';
// languageOptions: { parser }

// Using legacy .eslintrc.cjs instead.
