Branching & PR workflow

- Use feature branches: feature/<area>-<short-desc> (e.g., feature/e2e-search-engine)
- Never push directly to `main`. Open a PR and let CI run lint/typecheck/tests.

Commit messages

- Use conventional commits: feat(scope): short description
